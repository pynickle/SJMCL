export enum PlayerType {
  Offline = "offline",
  ThirdParty = "3rdparty",
  Microsoft = "microsoft",
}

export enum PresetRole {
  Steve = "steve",
  Alex = "alex",
}

export enum SkinModel {
  Default = "DEFAULT",
  Slim = "SLIM",
}

export enum TextureType {
  Skin = "SKIN",
  Cape = "CAPE",
}
